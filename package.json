{
  "name": "webrtc-test",
  "version": "1.0.0",
  "description": "WebRTC(Coturn) connection test - connection state and process visibility",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "^5.3.0",
    "vite": "^5.0.0"
  }
}
